<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Minifb</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application','vendor/modernizr', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>
    <nav class="top-bar" data-topbar role="navigation">
        <ul class="title-area">
            <li class="name">
                <h1><a href="/homepage">Mini Facebook</a></h1>
            </li>
             <!-- Remove the class "menu-icon" to get rid of menu icon. Take out "Menu" to just have icon alone -->
             <li class="toggle-topbar menu-icon"><a href="#"><span>Menu</span></a></li>
       </ul>

        <section class="top-bar-section">
            <!-- Right Nav Section -->
            <ul class="right">
                <li class="has-form">
                <% if user_signed_in?%>
                   <%=link_to 'sign out',destroy_user_session_path ,method: "delete" %>
                    <% else %>
                        <%=link_to 'sign in',new_user_session_path %>
                    <% end%>
                  </li>
            </ul>
<!-- Left Nav Section -->
    <% if current_user %>
        <ul class="left">
            <li class="has-form">
                <div class="row collapse">
                    <div class="large-8 small-9 columns">
                        <%= form_tag '/test',method: :get do%>
                            <%= autocomplete_field_tag 'name', '', users_autocomplete_user_name_path,  placeholder: "search by name" %>
                       
                    </div>
                    <div class="large-4 small-3 columns">
                        <%= submit_tag 'Search' %>
                    </div>
                <% end %>
                </div>
            </li>
        </ul>
    <% end %>
  </section>
</nav>




<%= yield %>

</body>
</html>
